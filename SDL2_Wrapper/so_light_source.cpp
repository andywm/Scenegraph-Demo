#include "so_includes.h"
#include "scene_graph.h"
/*
namespace Environment
{
	namespace Object
	{
		//make a directional light
		LightSource::LightSource(const Properties & properties,
			const glm::vec3 direction)
			:
			Base(glm::vec3(0), glm::vec3(0), glm::vec3(1))
		{
			
		}

		//make a point light
		LightSource::LightSource(const Properties & properties,
			const glm::vec3 position, float attenuation)
			:
			Base(position, glm::vec3(0), glm::vec3(1))
		{
			
		}

		//make a spot light
		LightSource::LightSource(const Properties & properties,
			const glm::vec3 position, const glm::vec3 direction,
			float attenuation, float halfArc, float exponent)
			:
			Base(position, direction, glm::vec3(1))
		{
			
		}

		void 
		LightSource::simulate(const double deltaTime)
		{
			return;
		}

		void 
		LightSource::buildSceneEntry(std::shared_ptr<SG_Base> target)
		{
			return;
		}

		void 
		LightSource::enable()
		{
			return;
		}

		void 
		LightSource::disable()
		{
			return;
		}
	}
}
*/